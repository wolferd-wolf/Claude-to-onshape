{
  "name": "onshape-claude-mcp",
  "version": "4.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "hono": "^4.9.0",
    "mcp-handler": "^2.1.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  },
  "scripts": {
    "postinstall": "node -e \"const fs=require('fs');const p='src/index.ts';const s=fs.readFileSync(p,'utf8');fs.writeFileSync(p,s.replaceAll('z.record(z.unknown())','z.record(z.string(),z.unknown())'))\""
  }
}
